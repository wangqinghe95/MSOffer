#include<iostream>
using namespace std;

// 本质：用减法代替除法

class Solution {
public:
    int divide(int a, int b) {
        // a,b 同号 sign 为 1，反之为负号
        int sign = (a > 0) ^ (b > 0) ? -1 : 1; // a > 0 ^ b > 0 = 0 | a < 0 ^ b < 0 = 0

        

    }
};

int main() {

}